- !policy
  id: conjur/authn-k8s/minikube/default
  body:
  - !policy
    id: apps
    annotations:
      description: Apps and services in Kubernetes "default" namespace.
    body:
    - &authorized-hosts
      - !host
        id: webapp
        annotations:
          kubernetes/dns-altnames: webapp.default.svc.cluster.local

      - !host webapp-summon

    - !grant
      role: !group /conjur/authn-k8s/minikube/default/clients
      members: *authorized-hosts
